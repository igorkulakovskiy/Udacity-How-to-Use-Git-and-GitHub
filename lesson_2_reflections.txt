What happens when you initialize a repository? Why do you need to do it?
- it starts be able to track versions and recceive commits
- it initiats be Git repository

How is the staging area different from the working directory and the repository? What value do you think it offers?
- working dir is phisical dir
- repository - is git dir
- staging area - dir for uncommited changes
- it provides ability to see uncommited changes

How can you use the staging area to make sure you have one commit per logical change?
- compare file added for commit with latest commit

What are some situations when branches would be helpful in keeping your history organized? How would branches help?
- create independent test version, which in case of successful can be combined with master version
- a tool of parallel work on the same app(code), implementing different features
- helps not to store all code changes, if commits refer to different features implementation.
- exmpl: cube is being modified by two people at the same time implementing different features